namespace duplicateFile.Classes
{
    public static class Tools
    {
        //public static bool isComparable(DataRowView drv1, DataRowView drv2)
        //{
        //    //use delta size for office file only
        //    var maxSize = ContentHash.isOfficeFile(drv1["Fichier"].ToString()) ? _config.MaxSizeDifference : 0.1;
        //}
    }
}